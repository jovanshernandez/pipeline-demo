apiVersion: v1
kind: Service
metadata:
  name: pipeline-demo
  labels:
    app.kubernetes.io/name: pipeline-demo
spec:
  selector:
    app.kubernetes.io/name: pipeline-demo
  ports:
    - name: http
      port: 80
      targetPort: http
